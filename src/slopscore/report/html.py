"""HTML report with highlighted spans. STUB — implemented in v0.2."""

from __future__ import annotations

from slopscore.models import Report


def to_html(report: Report) -> str:
    raise NotImplementedError("HTML reports with highlighted spans arrive in v0.2.")
